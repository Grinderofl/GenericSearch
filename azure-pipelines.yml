# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

trigger:
  branches:
    include:
      - master
      - release/*

stages:
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Compile
        displayName: 'Compile'
        dependsOn: [  ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile --skip'
      - job: Test
        displayName: 'Test'
        dependsOn: [ Compile ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd Test --skip'
      - job: Pack
        displayName: 'Pack'
        dependsOn: [ Compile, Test ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd VersionInfo Pack --skip'
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: artifacts
              pathtoPublish: 'artifacts'
      - job: TagRelease
        displayName: 'TagRelease'
        dependsOn: [ Pack ]
        steps:
          - task: CmdLine@2
            inputs:
              script: './build.cmd VersionInfo TagRelease --skip'
